# URL del backend
backend.url=http://localhost:8080
backend.health.endpoint=/actuator/health

# Usuarios de prueba (si requiere autenticación)
test.user.username=testuser
test.user.password=testpass

# Configuración de escenarios
scenario.normal.users=50
scenario.normal.rampup=60
scenario.normal.duration=300

scenario.peak.users=200
scenario.peak.rampup=120
scenario.peak.duration=600

scenario.stress.users=500
scenario.stress.rampup=180
scenario.stress.duration=900

# Umbrales de validación
max.avg.response.time.ms=1000
max.error.percentage=1.0
min.throughput.per.second=10

